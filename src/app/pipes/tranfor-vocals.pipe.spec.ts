import { TranforVocalsPipe } from './tranfor-vocals.pipe';

describe('TranforVocalsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranforVocalsPipe();
    expect(pipe).toBeTruthy();
  });
});
